check process <%= fetch(:sidekiq_name) %> with pidfile <%= fetch(:sidekiq_pid) %>
  start program = "service <%= fetch(:sidekiq_name) %> start" with timeout 90 seconds
  stop program = "service <%= fetch(:sidekiq_name) %> stop" with timeout 90 seconds
  if totalmem is greater than 200 MB for 2 cycles then restart # eating up memory?
